match_by_values <- function(survey_data, variable1, variable2, values)
{
  
}

greater_than <- function(survey_data, variable, boundary)
{
  
}

less_than <- function(survey_data, variable, boundary)
{
  
}

enforce_codes <- function(survey_data, variable, codes)
{
  
}

insuficient_observations <- function(survey_data, variable, code, minimum)
{
  
}

find_by_value <- function(survey_data, value, variable=TRUE)
{
  
}
